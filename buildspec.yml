version: 0.2
 
phases:
  install:
    commands:
      - |
        npm install
 
  pre_build:
    commands:
      - |
        npm test
 
  build:
    commands:
      - |
        echo 'no build'
 
artifacts:
  files:
    - "**/*"